export async function evaluateKnock(userId: string, ipPrefix: string, country: string) {
  void userId;
  void ipPrefix;
  void country;
  // In Phase 2, this is a skeleton for the suspicious login rules
  // Return 'none' for now to allow login to proceed
  return 'none';
}
