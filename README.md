# Slow Light spec bundle

Unzip at the repository root. `AGENTS.md`, config, tokens, schemas, DB migration and threat model are ready to use; `docs/spec/` holds the specification split per document; `docs/agent/MASTER_BUILD_PROMPT.md` is the prompt to give Antigravity.

Six working docs live in `docs/`: PRD.md, architecture.md, rules.md, design.md, task.md, memory.md.
